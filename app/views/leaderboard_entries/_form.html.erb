<%= form_with(model: leaderboard_entry, local: true) do |form| %>
  <% if leaderboard_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leaderboard_entry.errors.count, "error") %> prohibited this leaderboard_entry from being saved:</h2>

      <ul>
      <% leaderboard_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :leaderboard_id %>
    <%= form.text_field :leaderboard_id, id: :leaderboard_entry_leaderboard_id %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :leaderboard_entry_username %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, id: :leaderboard_entry_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
